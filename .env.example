# Copy this file to `.env.local` and adjust the values as needed.
# Currently this project does not require any environment variables.

# Example: override the ISR revalidation time
# REVALIDATE_SECONDS=60