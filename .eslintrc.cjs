module.exports = {
  root: true,
  extends: [
    // Use Next.js' recommended rules including core web vitals
    'next/core-web-vitals',
    // Disable formatting rules that conflict with Prettier
    'prettier',
  ],
  rules: {
    // Place to specify additional ESLint rules if needed
  },
};